#ifndef COMMON_H
#define COMMON_H

const char* SHM_NAME = "/my_shm_lab1";  // 3. const：定义常量不可修改；4. 字符指针：char 是基础类型（字符），* 表示这是一个指针。合起来表示这个变量存储的是一个内存地址，该地址指向一个字符序列（字符串）
const int SHM_SIZE = 1024;

#endif


#ifndef 宏名
// 内部逻辑
#endif

